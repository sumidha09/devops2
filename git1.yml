---
 - hosts: slave1
   become: true
   tasks:
     - name: installing git
       yum:
         name: git
         state: installed
     - name: start the service
       service:
         name: httpd
         state: started
      
